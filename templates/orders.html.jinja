<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>My Orders | Astrielle</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body class="bg-[#F9F9E0] min-h-screen text-[#333333] font-sans">

    {% include 'components/navbar.html.jinja' %}

    <main class="w-full max-w-5xl mx-auto px-6 py-16">

        <h1 class="text-4xl md:text-5xl font-bold mb-12 text-[#4A4A4A] text-center">My Orders</h1>

        {% if order|length > 0 %}

        <div class="space-y-8">

            {% for item in order %}
            <div class="bg-white rounded-3xl shadow-lg border border-[#E5E5CA] p-8">

              
                <div class="flex flex-col md:flex-row justify-between items-start md:items-center border-b border-gray-200 pb-6 mb-6">
                    <div>
                        <p class="font-semibold text-2xl text-[#4A4A4A]">Order #{{ item['ID'] }}</p>
                        <p class="text-gray-500 mt-1">Placed on {{ item["Timestamp"].strftime("%b %d") }}</p>
                    </div>

                    <div class="text-right mt-4 md:mt-0">
                        <p class="font-bold text-2xl text-[#E594A1]">Total: ${{ item["Total"] }}</p>
                    </div>
                </div>

               
                <p class="font-semibold text-gray-800">Items: {{ item["Quantity"] }}</p>

            </div>
            {% endfor %}

        </div>

        {% else %}

        <div class="flex flex-col items-center justify-center mt-24 text-center">
            <h2 class="text-3xl font-bold mb-4 text-[#4A4A4A]">No orders yet</h2>
            <p class="text-gray-500 mb-6">
                When you place an order, it will show up here.
            </p>

            <img src="/static/empty_cart.gif" class="w-64 mb-6" alt="No Orders">

            <a href="/browse"
               class="bg-[#E594A1] px-8 py-4 rounded-full text-white hover:bg-[#D48390] font-bold transition shadow-md">
                Start Shopping
            </a>
        </div>

        {% endif %}

    </main>

</body>
</html>
