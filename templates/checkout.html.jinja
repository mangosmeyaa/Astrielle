<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Checkout | Astrielle</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body class="bg-[#F9F9E0] text-[#333333] font-sans">

{% include "components/navbar.html.jinja" %}

<form method="post" action="/checkout">
    <div class="max-w-6xl mx-auto grid grid-cols-1 lg:grid-cols-3 gap-10 px-6 py-10">

        
        <div class="lg:col-span-2 bg-white p-8 rounded-3xl shadow-sm border border-[#E5E5CA]">
            <h2 class="text-3xl font-bold mb-8 text-[#4A4A4A]">Checkout</h2>

            <div class="space-y-4">
                <input name="full_name" required placeholder="Full Name"
                    class="w-full border rounded-xl px-4 py-3">

                <input name="address" required placeholder="Street Address"
                    class="w-full border rounded-xl px-4 py-3">

                <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
                    <input name="city" required placeholder="City"
                        class="border rounded-xl px-4 py-3">
                    <input name="state" placeholder="State"
                        class="border rounded-xl px-4 py-3">
                    <input name="zip" required placeholder="Zip Code"
                        class="border rounded-xl px-4 py-3">
                </div>
            </div>
        </div>

        
        <div class="bg-white p-8 rounded-3xl shadow-sm border h-fit sticky top-10">
            <h2 class="text-2xl font-bold mb-6">Order Summary</h2>

            <div class="space-y-6 mb-8">
                {% for x in cart %}
                <div class="flex justify-between items-center">
                    <div class="flex items-center gap-4">
                        <img
                          src="{{ x['Image']}}"
                          alt="{{ x['Name'] }}"
                          class="w-16 h-16 rounded-xl border object-cover">

                        <div>
                            <p class="font-semibold">{{ x['Name'] }}</p>
                            <p class="text-xs uppercase">Qty {{ x['Quantity'] }}</p>
                        </div>
                    </div>
                    <span>${{ x['Price'] * x['Quantity'] }}</span>
                </div>
                {% endfor %}
            </div>

            <div class="border-t pt-6 space-y-3">
                <div class="flex justify-between">
                    <span>Subtotal</span>
                    <span>${{ total }}</span>
                </div>
                <div class="flex justify-between font-bold text-xl">
                    <span>Total</span>
                    <span>${{ total }}</span>
                </div>
            </div>

            <button type="submit"
                class="mt-8 w-full bg-[#E594A1] text-white py-4 rounded-full font-bold">
                Place Order
            </button>
        </div>

    </div>
</form>

</body>
</html>
